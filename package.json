{
  "name": "strellis",
  "private": true,
  "workspaces": [
    "editor"
  ],
  "scripts": {
    "editor:dev": "bun run --filter editor dev",
    "editor:build": "bun run --filter editor build",
    "editor:preview": "bun run --filter editor preview",
    "editor:add": "bun add --cwd editor"
  }
}
